package com.ampairs.claim.config

/** UID prefixes for the claim (trade-scheme claims & settlement) module. */
object Constants {
    const val CLAIM_PREFIX = "SCL"
    const val SETTLEMENT_PREFIX = "CST"
}

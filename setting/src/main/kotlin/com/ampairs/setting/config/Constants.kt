package com.ampairs.setting.config

object Constants {
    const val STORE_SETTING_PREFIX = "STG"

    /** Mobile `SyncEntity` code used for real-time change broadcasts. */
    const val SYNC_ENTITY_CODE = "setting"
}

package com.ampairs.trade.config

/** UID prefixes for the trade (cross-tenant network & consent) module. */
object Constants {
    const val NETWORK_PREFIX = "TND"
    const val LINK_PREFIX = "TLK"
    const val NETWORK_RETAILER_PREFIX = "NRT"
    const val NETWORK_BRAND_PREFIX = "NBR"
    const val NETWORK_PRODUCT_PREFIX = "NPR"
    const val SCHEME_PUBLICATION_PREFIX = "SPB"
    const val PRIMARY_ORDER_PREFIX = "POL"
}
